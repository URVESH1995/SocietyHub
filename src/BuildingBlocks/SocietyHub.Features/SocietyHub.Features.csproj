<Project Sdk="Microsoft.NET.Sdk">

  <ItemGroup>
    <ProjectReference Include="..\SocietyHub.SharedKernel\SocietyHub.SharedKernel.csproj" />
    <ProjectReference Include="..\SocietyHub.Caching\SocietyHub.Caching.csproj" />
  </ItemGroup>

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

  <ItemGroup>
    <!-- The bucket function is internal because it is an implementation detail, but it is
         also the part most worth testing directly: a rollout that hashes badly is invisible
         from the outside until it reaches the wrong societies. -->
    <InternalsVisibleTo Include="SocietyHub.Features.Tests" />
  </ItemGroup>

  <ItemGroup>
    <FrameworkReference Include="Microsoft.AspNetCore.App" />
  </ItemGroup>

</Project>
