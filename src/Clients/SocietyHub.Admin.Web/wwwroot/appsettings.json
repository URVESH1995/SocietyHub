{
  "//": "Where the console reaches the platform. The gateway is the only publicly addressable component; everything behind it is reached through service discovery inside the cluster network. Its port is pinned in the Aspire AppHost precisely so this value can exist — a WebAssembly build cannot discover a port that changes on every run.",
  "Gateway": {
    "BaseAddress": "http://localhost:5280/"
  },
  "//ClientVersions": "Mirrors the server-side deprecation policy so the console can warn before it is refused. See docs/API-VERSIONING.md.",
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "System.Net.Http.HttpClient": "Warning"
    }
  }
}
