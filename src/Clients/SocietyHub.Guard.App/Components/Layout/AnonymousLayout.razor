@inherits LayoutComponentBase

@*
    Signed-out shell for the gate tablet: the page and nothing else.

    No navigation and no authenticated calls, because everything the gate screen does needs a
    token and a shift PIN.
*@

<main class="sh-shell sh-shell--anonymous">
    @Body
</main>
